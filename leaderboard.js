(()=>{const KEY='volleyverseLeaderboard';const seed=[{name:'AstraNova',rating:1820,wins:74,season:1},{name:'SkySpikers',rating:1765,wins:68,season:1},{name:'NetKings',rating:1690,wins:61,season:1},{name:'VolleyStorm',rating:1610,wins:55,season:1},{name:'BlueAces',rating:1540,wins:49,season:1}];const load=()=>{try{const s=JSON.parse(localStorage.getItem(KEY)||'null');return Array.isArray(s)?s:seed.map(x=>({...x}))}catch{return seed.map(x=>({...x}))}};const save=s=>localStorage.setItem(KEY,JSON.stringify(s));const player=()=>{const r=window.VVRanked?.load?.()||{rating:0,wins:0};return{name:'YOU',rating:r.rating,wins:r.wins,season:r.season||1}};const update=()=>{const p=player(),all=load().filter(x=>x.name!=='YOU');all.push(p);all.sort((a,b)=>b.rating-a.rating);save(all.slice(0,100));return all};const render=()=>{const list=document.getElementById('leaderboardList'),season=document.getElementById('leaderboardSeason');if(!list)return;const all=update(),p=player();if(season)season.textContent='SEASON '+String(p.season).padStart(2,'0');list.innerHTML=all.slice(0,20).map((x,i)=>'<div class="leader-row '+(x.name==='YOU'?'you':'')+'"><b>#'+(i+1)+'</b><span>'+x.name+'</span><strong>'+x.rating+' RP</strong><small>'+x.wins+' W</small></div>').join('')};const open=()=>{let m=document.getElementById('leaderboardModal');if(!m){m=document.createElement('div');m.id='leaderboardModal';m.className='modal';m.innerHTML='<div class="modal-panel leaderboard-panel"><button class="modal-close" id="leaderClose">×</button><span class="eyebrow">GLOBAL LEADERBOARD</span><h2>WORLD RANKINGS</h2><p id="leaderboardSeason">SEASON 01</p><div id="leaderboardList" class="leaderboard-list"></div><button class="primary" id="leaderBack">BACK</button><p class="leader-note">Prototype leaderboard uses local ranked data. Server-synchronized global rankings will be connected during online multiplayer development.</p></div>';document.body.appendChild(m);document.getElementById('leaderClose').onclick=()=>m.classList.add('hidden');document.getElementById('leaderBack').onclick=()=>m.classList.add('hidden')}render();m.classList.remove('hidden')};window.VVLeaderboard={load,save,update,open}})();