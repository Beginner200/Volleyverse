(()=>{const KEY='volleyverseprofile';const defaults={username:'PLAYER',country:'PH',avatar:'default',bio:'VOLLEYVERSE PLAYER',joined:Date.now()};const load=()=>{try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...defaults}}};const save=s=>localStorage.setItem(KEY,JSON.stringify(s));const open=()=>{let m=document.getElementById('profileModal');if(!m){m=document.createElement('div');m.id='profileModal';m.className='modal';m.innerHTML='<div class="modal-panel profile-panel"><button class="modal-close" id="profileClose">×</button><span class="eyebrow">PLAYER IDENTITY</span><h2>PROFILE</h2><label>USERNAME<input id="profileName" maxlength="18"></label><label>COUNTRY<input id="profileCountry" maxlength="3"></label><label>BIO<textarea id="profileBio" maxlength="80"></textarea></label><button class="primary" id="profileSave">SAVE PROFILE</button><p class="profile-note">Profile foundation for future accounts, friends, clubs, rankings and cloud saves.</p></div></div>';document.body.appendChild(m);document.getElementById('profileClose').onclick=()=>m.classList.add('hidden');document.getElementById('profileSave').onclick=()=>{const s=load();s.username=document.getElementById('profileName').value.trim()||'PLAYER';s.country=document.getElementById('profileCountry').value.trim().toUpperCase().slice(0,3)||'PH';s.bio=document.getElementById('profileBio').value.trim()||defaults.bio;save(s);m.classList.add('hidden')}}const s=load();document.getElementById('profileName').value=s.username;document.getElementById('profileCountry').value=s.country;document.getElementById('profileBio').value=s.bio;m.classList.remove('hidden')};window.VVProfile={load,save,open}})();