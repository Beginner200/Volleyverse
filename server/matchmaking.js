function groupQueue(queue){const groups=new Map();for(const p of queue){const key=p.partyId||p.id;if(!groups.has(key))groups.set(key,[]);groups.get(key).push(p)}return [...groups.values()]}function buildTeams(groups){const teams=[];let cur=[];for(const g of groups){if(cur.length+g.length<=6){cur.push(...g)}else if(cur.length){teams.push(cur);cur=[...g]}else{teams.push(g)}}if(cur.length)teams.push(cur);return teams}module.exports={groupQueue,buildTeams};