(()=>{
const KEY='volleyverseChampionship';
const stages=['GROUP STAGE','ROUND OF 16','QUARTERFINAL','SEMIFINAL','WORLD FINAL'];
const defaults={stage:0,wins:0,losses:0,champion:false,points:0};
const load=()=>{try{return {...defaults,...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {...defaults}}};
const save=s=>localStorage.setItem(KEY,JSON.stringify(s));
const record=won=>{const s=load();if(won){s.wins++;s.points+=3;s.stage=Math.min(4,s.stage+1);if(s.stage===4)s.champion=true;if(window.VVEconomy?.addCoins)window.VVEconomy.addCoins(150)}else{s.losses++;s.points+=1}save(s);render();return s};
const reset=()=>{save({...defaults});render()};
const render=()=>{const s=load(),st=document.getElementById('wcStage'),wins=document.getElementById('wcWins'),loss=document.getElementById('wcLosses'),pts=document.getElementById('wcPoints'),list=document.getElementById('wcStages');if(st)st.textContent=s.champion?'WORLD CHAMPION':stages[s.stage];if(wins)wins.textContent=s.wins;if(loss)loss.textContent=s.losses;if(pts)pts.textContent=s.points;if(list)list.innerHTML=stages.map((x,i)=>'<div class="wc-stage '+(i<=s.stage?'active':'')+'"><b>'+x+'</b><span>'+((i<s.stage)?'CLEARED':i===s.stage?'CURRENT':'LOCKED')+'</span></div>').join('')};
const open=()=>{let m=document.getElementById('worldChampionshipModal');if(!m){m=document.createElement('div');m.id='worldChampionshipModal';m.className='modal';m.innerHTML='<div class="modal-panel wc-panel"><button class="modal-close" id="wcClose">×</button><span class="eyebrow">WORLD CHAMPIONSHIP</span><h2 id="wcStage">GROUP STAGE</h2><p>Climb through the global tournament bracket and fight for the championship.</p><div id="wcStages" class="wc-stages"></div><div class="wc-stats"><div><b id="wcWins">0</b><span>WINS</span></div><div><b id="wcLosses">0</b><span>LOSSES</span></div><div><b id="wcPoints">0</b><span>POINTS</span></div></div><div class="wc-actions"><button class="primary" id="wcWin">RECORD WIN</button><button class="glass" id="wcLoss">RECORD LOSS</button><button class="glass" id="wcReset">RESET EVENT</button></div><p class="wc-note">Tournament progression foundation. Full online brackets, regional matchmaking and live championship opponents will be connected during online multiplayer development.</p></div></div>';document.body.appendChild(m);document.getElementById('wcClose').onclick=()=>m.classList.add('hidden');document.getElementById('wcWin').onclick=()=>record(true);document.getElementById('wcLoss').onclick=()=>record(false);document.getElementById('wcReset').onclick=reset}render();m.classList.remove('hidden')};
window.VVWorldChampionship={load,save,record,open,reset};
})();