(()=>{const KEY='volleyverseParty';const load=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')||{name:'MY PARTY',leader:'PLAYER',members:[],status:'OPEN'}}catch{return {name:'MY PARTY',leader:'PLAYER',members:[],status:'OPEN'}}};const save=s=>localStorage.setItem(KEY,JSON.stringify(s));const open=()=>{let m=document.getElementById('partyModal');if(!m){m=document.createElement('div');m.id='partyModal';m.className='modal';m.innerHTML='<div class="modal-panel party-panel"><button class="modal-close" id="partyClose">×</button><span class="eyebrow">TEAM LOBBY</span><h2>PARTY & LOBBY</h2><p>Build a squad with friends before entering a match.</p><div class="party-status"><b id="partyStatus">OPEN</b><span id="partyCount">1 / 6 PLAYERS</span></div><div id="partyMembers" class="party-members"></div><div class="party-actions"><button class="primary" id="partyInvite">INVITE FRIEND</button><button class="glass" id="partyReady">READY</button><button class="glass" id="partyLeave">LEAVE PARTY</button></div><p class="party-note">Prototype lobby foundation for friends, private matches, ranked queues and future online 6v6.</p></div></div>';document.body.appendChild(m);document.getElementById('partyClose').onclick=()=>m.classList.add('hidden');document.getElementById('partyInvite').onclick=()=>{const s=load();if(s.members.length>=5)return;const n='Friend '+(s.members.length+1);s.members.push({name:n,status:'INVITED'});save(s);render()};document.getElementById('partyReady').onclick=()=>{const s=load();s.status=s.status==='READY'?'OPEN':'READY';save(s);render()};document.getElementById('partyLeave').onclick=()=>{save({name:'MY PARTY',leader:'PLAYER',members:[],status:'OPEN'});render()}}render();m.classList.remove('hidden')};const render=()=>{const s=load(),members=[{name:s.leader||'PLAYER',status:s.status==='READY'?'READY':'LEADER'},...(s.members||[])],box=document.getElementById('partyMembers');if(box)box.innerHTML=members.map(x=>'<div class="party-member"><b>'+x.name+'</b><span>'+x.status+'</span></div>').join('');document.getElementById('partyCount')?.replaceChildren(document.createTextNode(members.length+' / 6 PLAYERS'));document.getElementById('partyStatus')?.replaceChildren(document.createTextNode(s.status))};window.VVParty={load,save,open,render}})();