(()=>{const KEY='volleyverseprivate';const load=()=>{try{return {...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return {}}};const save=s=>localStorage.setItem(KEY,JSON.stringify(s));const create=name=>{const s=load();s.room=s.room||Math.random().toString(36).slice(2,8).toUpperCase();s.name=name||'PRIVATE ROOM';s.host=true;s.status='OPEN';save(s);render();return s};const join=(code)=>{const s=load();s.room=(code||'').trim().toUpperCase();s.host=false;s.status=s.room?'JOINED':'IDLE';save(s);render();return s};const leave=()=>{save({});render()};const render=()=>{const s=load(),code=document.getElementById('privateCode'),status=document.getElementById('privateStatus');if(code)code.textContent=s.room||'------';if(status)status.textContent=s.status||'IDLE'};const open=()=>{let m=document.getElementById('privateModal');if(!m){m=document.createElement('div');m.id='privateModal';m.className='modal';m.innerHTML='<div class="modal-panel private-panel"><button class="modal-close" id="privateClose">×</button><span class="eyebrow">PRIVATE MATCH</span><h2>FRIEND ROOM</h2><div class="private-code" id="privateCode">------</div><p id="privateStatus">IDLE</p><div class="private-actions"><button class="primary" id="privateCreate">CREATE ROOM</button><input id="privateInput" maxlength="8" placeholder="ROOM CODE"><button class="glass" id="privateJoin">JOIN</button><button class="glass" id="privateLeave">LEAVE</button></div><p class="private-note">Private-match foundation for friends. Real invitation delivery, room authority and server/peer synchronization will connect during online multiplayer.</p></div></div>';document.body.appendChild(m);document.getElementById('privateClose').onclick=()=>m.classList.add('hidden');document.getElementById('privateCreate').onclick=()=>create('FRIEND ROOM');document.getElementById('privateJoin').onclick=()=>join(document.getElementById('privateInput').value);document.getElementById('privateLeave').onclick=leave}render();m.classList.remove('hidden')};window.VVPrivateMatch={load,save,create,join,leave,open}})();